name: old_permic_ocr_mobile
description: Generic, offline-first Flutter OCR runtime for self-describing model packages.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  archive: ^3.6.1
  crypto: ^3.0.6
  dio: ^5.8.0+1
  file_picker: ^8.3.7
  image_picker: ^1.1.2
  image: ^4.3.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  onnxruntime: ^1.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
